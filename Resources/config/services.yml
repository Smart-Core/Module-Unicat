parameters:
#    unicat.example.class: SmartCore\Module\Unicat\Example

services:
    unicat:
        class: SmartCore\Module\Unicat\Service\UnicatService
        arguments: [ @doctrine, @form.factory, @smart_media, @security.token_storage ]
        calls:
            - [ setContainer, [ @service_container ] ]
