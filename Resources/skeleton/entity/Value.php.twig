<?php

namespace {{ namespace }};

use Doctrine\ORM\Mapping as ORM;
use SmartCore\Module\Unicat\Model\Value{{ type }}Model;

/**
 * @ORM\Entity
 * @ORM\Table(name="{{ table_prefix ~ configuration.name ~ '_values__' ~ name }}",
 *      indexes={
 *          @ORM\Index(columns={"value"}),
 *      }
 * )
 */
class Value{{ name_camel_case }} extends Value{{ type }}Model
{
}
